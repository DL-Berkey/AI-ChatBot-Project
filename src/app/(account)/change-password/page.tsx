import ChangePasswordForm from "@/components/change-password/ChangePasswordForm";

const page = () => {
    return (
        <main>
            <ChangePasswordForm />
        </main>
    );
};

export default page;
