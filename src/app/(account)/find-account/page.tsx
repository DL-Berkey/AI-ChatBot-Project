import { Card, CardContent, CardHeader } from "@/components/ui/card";

const page = () => {
    return (
        <Card className="mx-auto w-96 mt-28 shadow-main">
            <CardHeader>아이디 찾기 / 비밀번호 변경</CardHeader>
            <CardContent>개발 중 입니다.</CardContent>
        </Card>
    );
};

export default page;
